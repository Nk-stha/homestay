# Project Structure Visualization

```
vanavasam-homestay/
│
├── 📄 Configuration Files
│   ├── package.json              # Dependencies & scripts
│   ├── tsconfig.json             # TypeScript config
│   ├── tsconfig.app.json         # App TypeScript config (with path aliases)
│   ├── vite.config.ts            # Vite bundler config
│   ├── tailwind.config.js        # Tailwind CSS config (design system)
│   ├── postcss.config.js         # PostCSS config
│   └── eslint.config.js          # ESLint rules
│
├── 📚 Documentation
│   ├── README.md                 # Project overview & setup
│   ├── design.md                 # 🌟 Complete design blueprint
│   ├── IMPLEMENTATION_GUIDE.md   # Step-by-step implementation
│   ├── PROJECT_SUMMARY.md        # High-level summary
│   └── QUICK_REFERENCE.md        # Developer quick reference
│
├── 🎨 Source Code (src/)
│   │
│   ├── 🏗️ App Configuration (app/)
│   │   ├── providers.tsx         # Global providers (Theme, etc.)
│   │   └── store.ts              # State management (if needed)
│   │
│   ├── 🖼️ Assets (assets/)
│   │   └── react.svg             # Static images, icons
│   │
│   ├── 🧩 Components (components/)
│   │   │
│   │   ├── ui/                   # Base UI components
│   │   │   ├── Button.tsx        # Primary & Ghost variants
│   │   │   └── Input.tsx         # Form inputs with floating labels
│   │   │
│   │   ├── layout/               # Layout components
│   │   │   ├── Navbar.tsx        # Navigation with scroll behavior
│   │   │   ├── Header.tsx        # Hero section
│   │   │   └── Footer.tsx        # Footer with wave divider
│   │   │
│   │   └── common/               # Reusable components
│   │       ├── ScrollToTop.tsx   # Scroll to top button
│   │       └── ContactButtons.tsx # Fixed contact buttons
│   │
│   ├── 🎯 Features (features/)
│   │   │
│   │   ├── auth/                 # Authentication feature
│   │   │   ├── api.ts            # Auth API calls
│   │   │   ├── components/       # Auth-specific components
│   │   │   ├── hooks.ts          # Auth hooks
│   │   │   ├── types.ts          # Auth types
│   │   │   └── pages/            # Auth pages
│   │   │
│   │   ├── users/                # Users feature
│   │   │   ├── api.ts
│   │   │   ├── components/
│   │   │   ├── hooks.ts
│   │   │   ├── types.ts
│   │   │   └── pages/
│   │   │
│   │   └── dashboard/            # 🌟 Main homestay feature
│   │       ├── api.ts
│   │       ├── types.ts          # Room, Package, Wildlife types
│   │       │
│   │       ├── components/       # Section components
│   │       │   ├── IntroSection.tsx
│   │       │   ├── TestimonialSection.tsx
│   │       │   ├── AccommodationsSection.tsx
│   │       │   ├── SafariSection.tsx
│   │       │   ├── PackagesSection.tsx
│   │       │   ├── HostsSection.tsx
│   │       │   └── WildlifeSection.tsx
│   │       │
│   │       └── pages/
│   │           └── HomePage.tsx  # Main landing page
│   │
│   ├── 🪝 Hooks (hooks/)
│   │   └── index.ts              # Global reusable hooks
│   │
│   ├── 📚 Libraries (lib/)
│   │   ├── axios.ts              # API client config
│   │   ├── animations.ts         # 🌟 Animation utilities
│   │   └── utils.ts              # Helper functions
│   │
│   ├── 🔧 Services (services/)
│   │   └── authService.ts        # Auth service (localStorage)
│   │
│   ├── 📝 Types (types/)
│   │   └── index.ts              # Global TypeScript types
│   │
│   ├── 🎨 Constants (constants/)
│   │   ├── index.ts              # App constants
│   │   └── design.ts             # 🌟 Design system constants
│   │
│   ├── 💅 Styles (styles/)
│   │   └── globals.css           # Global styles
│   │
│   ├── App.tsx                   # Main App component
│   ├── main.tsx                  # React entry point
│   └── index.css                 # 🌟 Tailwind + custom utilities
│
├── 🌐 Public (public/)
│   └── vite.svg                  # Public assets
│
├── 📦 Build Output (dist/)        # Generated by `npm run build`
│   ├── index.html
│   ├── assets/
│   │   ├── index-[hash].css
│   │   └── index-[hash].js
│   └── ...
│
└── 🔧 Node Modules (node_modules/) # Dependencies

```

## 🎨 Design System Flow

```
design.md (Blueprint)
    ↓
tailwind.config.js (Colors, Fonts, Animations)
    ↓
src/constants/design.ts (TypeScript Constants)
    ↓
src/index.css (Utilities: grain-texture, card-lift, etc.)
    ↓
Components (Use design system classes)
```

## 🔄 Data Flow

```
User Interaction
    ↓
Component (React)
    ↓
Hook (if needed)
    ↓
Service/API (if needed)
    ↓
State Update
    ↓
Re-render with Animation
```

## 🎯 Feature-Based Architecture

Each feature is self-contained:

```
features/[feature-name]/
├── api.ts          # API calls for this feature
├── components/     # Feature-specific components
├── hooks.ts        # Feature-specific hooks
├── types.ts        # Feature-specific types
└── pages/          # Feature pages
```

Benefits:
- ✅ Easy to find related code
- ✅ Can be extracted as a package
- ✅ Clear boundaries
- ✅ Scalable

## 🚀 Build Process

```
Source Code (src/)
    ↓
TypeScript Compilation (tsc)
    ↓
Vite Bundling
    ↓
Tailwind CSS Processing
    ↓
PostCSS Optimization
    ↓
Minification & Tree Shaking
    ↓
Output (dist/)
```

## 📱 Component Hierarchy

```
App.tsx
├── Navbar
├── HomePage
│   ├── Header (Hero)
│   ├── IntroSection
│   ├── TestimonialSection
│   ├── AccommodationsSection
│   ├── SafariSection
│   ├── PackagesSection
│   ├── HostsSection
│   └── WildlifeSection
├── Footer
├── ScrollToTop
└── ContactButtons
```

## 🎨 Styling Layers

```
Layer 1: Tailwind Base
    ↓
Layer 2: Design System (tailwind.config.js)
    ↓
Layer 3: Custom Utilities (index.css)
    ↓
Layer 4: Component Styles (className)
    ↓
Layer 5: Inline Styles (style prop, rare)
```

## 🔐 Type Safety Flow

```
types/index.ts (Global Types)
    ↓
features/[feature]/types.ts (Feature Types)
    ↓
Component Props (Interface)
    ↓
TypeScript Validation
    ↓
Runtime Safety
```

## 📊 Performance Strategy

```
Code Splitting
    ↓
Lazy Loading (Images, Components)
    ↓
Tree Shaking (Remove unused code)
    ↓
Minification
    ↓
Compression (Gzip)
    ↓
CDN Delivery
```

## 🎬 Animation Pipeline

```
User Scroll/Interaction
    ↓
Intersection Observer (lib/animations.ts)
    ↓
CSS Class Toggle
    ↓
Tailwind Animation Classes
    ↓
GPU-Accelerated Animation
    ↓
Smooth Visual Feedback
```

## 🔍 Import Path Resolution

```
@/components/ui/Button
    ↓
tsconfig.app.json (baseUrl + paths)
    ↓
vite.config.ts (resolve.alias)
    ↓
Resolved to: src/components/ui/Button.tsx
```

## 📝 Development Workflow

```
1. Read design.md
    ↓
2. Check IMPLEMENTATION_GUIDE.md
    ↓
3. Use QUICK_REFERENCE.md
    ↓
4. Write component
    ↓
5. Use design system classes
    ↓
6. Test responsiveness
    ↓
7. Check accessibility
    ↓
8. Commit changes
```

---

**Legend:**
- 🌟 = Critical files for design system
- 📄 = Configuration
- 📚 = Documentation
- 🎨 = Styling/Design
- 🧩 = Components
- 🎯 = Features
- 🔧 = Utilities
